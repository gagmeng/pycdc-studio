name: Legacy Windows Release Workflow (disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: windows-latest
    steps:
      - run: echo "This workflow has been replaced by .github/workflows/release.yml"
